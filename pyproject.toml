[tool.ruff]
line-length = 88
target-version = "py310"

[tool.ruff.lint]
select = [
    "B",
    "E",
    "F",
    "I",
    "UP",
]

[tool.ruff.lint.per-file-ignores]
"scripts/*.py" = ["E402", "I001"]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
