# For docker run on the existing Compose network; never use localhost for PostgreSQL here.
DATABASE_URL=postgresql://traffic_user:change_me_for_local_development@postgres:5432/traffic_monitoring
API_DEVICE=cuda:0
API_CORS_ORIGINS=http://localhost:5173
API_VIDEO_WORKERS=1
